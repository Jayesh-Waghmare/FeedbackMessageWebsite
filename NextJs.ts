// Regex is a sequence of characters that defines a search pattern used for matching, finding, or manipulating text.

// Zod is a TypeScript-first **schema validation library** that allows developers to define schemas for their data, validate data at runtime, and leverage TypeScript's type system for type safety, offering both compile-time and runtime validation. 

// NextJs is a edge time framework means whenever there is a request from the user then it gets executed/runs

// re_jH1LHfFT_JkGEAazje1eDkMRRSqwjX85X - Resend Email API Key password

// Database Connection is always at route as NextJS works on edge